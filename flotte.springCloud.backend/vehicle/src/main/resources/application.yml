server:
    port: 8484
spring:
    application:
        name: VEHICULE-SERVICE
keycloak:
    realm: flotte-realm
    resource: flotte-client
    bearer-only: true
    auth-server-url: http://localhost:9090/
    ssl-required: none
